<%- include('../partials/header', { title: 'Projeyi Düzenle' }) %>

<div class="max-w-3xl mx-auto py-12">
    <div class="mb-8">
        <a href="/projects/<%= project._id %>" class="text-gray-500 hover:text-white transition-colors flex items-center gap-2 mb-4">
            <i class="fas fa-arrow-left"></i> Projeye Dön
        </a>
        <h1 class="text-4xl font-extrabold text-white mb-2">Projeyi Düzenle</h1>
        <p class="text-gray-400 font-mono">ID: <%= project._id %></p>
    </div>

    <div class="bg-dark-800/50 glass-panel border border-white/10 p-8 rounded-3xl shadow-2xl">
        <form action="/projects/<%= project._id %>?_method=PUT" method="POST" enctype="multipart/form-data" class="space-y-6">
            <div>
                <label class="block text-sm font-bold text-gray-300 mb-2">Proje Başlığı <span class="text-brand-500">*</span></label>
                <input type="text" name="title" value="<%= project.title %>" required 
                    class="w-full bg-dark-900 border border-dark-700 text-white px-4 py-3 rounded-xl focus:outline-none focus:border-brand-500 transition-all">
            </div>

            <div>
                <label class="block text-sm font-bold text-gray-300 mb-2">Açıklama <span class="text-brand-500">*</span></label>
                <textarea name="description" rows="6" required 
                    class="w-full bg-dark-900 border border-dark-700 text-white px-4 py-3 rounded-xl focus:outline-none focus:border-brand-500 transition-all resize-none"><%= project.description %></textarea>
            </div>

            <div>
                <label class="block text-sm font-bold text-gray-300 mb-2">Teknolojiler (Virgülle ayırın)</label>
                <div class="relative">
                    <span class="absolute left-4 top-3.5 text-gray-600"><i class="fas fa-code"></i></span>
                    <input type="text" name="technologies" value="<%= project.technologies.join(', ') %>" 
                        class="w-full bg-dark-900 border border-dark-700 text-white pl-10 pr-4 py-3 rounded-xl focus:outline-none focus:border-brand-500 transition-all">
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                    <label class="block text-sm font-bold text-gray-300 mb-2">Repo URL</label>
                    <div class="relative">
                        <span class="absolute left-4 top-3.5 text-gray-600"><i class="fab fa-github"></i></span>
                        <input type="url" name="repoUrl" value="<%= project.repoUrl %>" 
                            class="w-full bg-dark-900 border border-dark-700 text-white pl-10 pr-4 py-3 rounded-xl focus:outline-none focus:border-brand-500 transition-all">
                    </div>
                </div>
                <div>
                    <label class="block text-sm font-bold text-gray-300 mb-2">Canlı URL</label>
                    <div class="relative">
                        <span class="absolute left-4 top-3.5 text-gray-600"><i class="fas fa-globe"></i></span>
                        <input type="url" name="liveUrl" value="<%= project.liveUrl %>" 
                            class="w-full bg-dark-900 border border-dark-700 text-white pl-10 pr-4 py-3 rounded-xl focus:outline-none focus:border-brand-500 transition-all">
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                 <div>
                    <label class="block text-sm font-bold text-gray-300 mb-2">Görünürlük</label>
                    <select name="visibility" class="w-full bg-dark-900 border border-dark-700 text-white px-4 py-3 rounded-xl focus:outline-none focus:border-brand-500 transition-all cursor-pointer">
                        <option value="public" <%= project.visibility == 'public' ? 'selected' : '' %>>Herkese Açık (Public)</option>
                        <option value="private" <%= project.visibility == 'private' ? 'selected' : '' %>>Özel (Private)</option>
                    </select>
                </div>
                <div>
                    <label class="block text-sm font-bold text-gray-300 mb-2">Yeni Kapak Resmi (Değiştirmek için)</label>
                    <label class="flex items-center justify-center w-full h-[50px] px-4 transition bg-dark-900 border-2 border-dark-700 border-dashed rounded-xl appearance-none cursor-pointer hover:border-brand-500 focus:outline-none">
                        <span class="flex items-center space-x-2">
                            <i class="fas fa-cloud-upload-alt text-gray-400"></i>
                            <span class="text-gray-400 font-medium">Dosya Seçin...</span>
                        </span>
                        <input type="file" name="image" accept="image/*" class="hidden">
                    </label>
                </div>
            </div>

            <% if(project.imagePath) { %>
                <div class="bg-dark-900 p-4 rounded-xl border border-white/10">
                    <p class="text-sm text-gray-400 mb-2">Mevcut Resim:</p>
                    <img src="<%= project.imagePath %>" alt="Mevcut Resim" class="h-32 rounded-lg object-cover">
                </div>
            <% } %>

            <div class="pt-6 border-t border-white/10 flex items-center justify-end gap-4">
                <a href="/projects/<%= project._id %>" class="px-6 py-3 bg-dark-800 text-gray-300 rounded-xl hover:bg-dark-700 transition-all font-medium">İptal</a>
                <button type="submit" class="px-8 py-3 bg-blue-600 hover:bg-blue-500 text-white font-bold rounded-xl shadow-[0_0_20px_rgba(59,130,246,0.3)] transition-all transform hover:scale-[1.02]">
                    Güncelle <i class="fas fa-save ml-2"></i>
                </button>
            </div>
        </form>
    </div>
</div>

<%- include('../partials/footer') %>